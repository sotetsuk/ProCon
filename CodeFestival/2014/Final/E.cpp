#include <iostream>
#include <cstdio>
#include <cstring>
#include <climits>
#include <string>
#include <algorithm>
#include <cmath>
#include <vector>
#include <stack>
#include <queue>
#include <map>
#include <sstream>
using namespace std;
typedef pair<int, int> pii;
typedef long long int ll;

const int MAX_N = 3000;

int N;
int R[MAX_N];

int main() {
    cin >> N;
    for(int i = 0; i < N; i++) {
        cin >> R[i];
    }

    int lb = -1e5;
    int ub = 1e5;

    while(ub - lb > 1) {
        int mid = (lb + ub) / 2;
        if()

    }

    return 0;
}
